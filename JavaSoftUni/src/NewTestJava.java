import java.util.Scanner;

public class NewTestJava {
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);
        int number = Integer.parseInt(scan.nextLine());

        System.out.printf("Line of code: %d", number);
    }
}
